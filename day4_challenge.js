function alphaCount (alphabet, text) {
  // Write your code here, and
  // return your final answer.
  alpha = {}
  
  for (var i = key.length - 1; i >= 0; i--) {
  	alpah[key[i].toLowerCase()] += 1
  };

  
}